torch>=2.1.0
torchaudio>=2.1.0
# transformers>=4.40.0 is required for MimiModel (kyutai/moshiko-pytorch-q8)
# and HuBERT-large support
transformers>=4.40.0
pyyaml>=6.0
numpy>=1.24.0
librosa>=0.10.0
pyworld>=0.3.4
tensorboard>=2.14.0

# Note: the moshi git repository is NO LONGER required.
# Mimi encoding is handled via the HuggingFace transformers MimiModel class.
# Install with:  pip install transformers>=4.40.0
